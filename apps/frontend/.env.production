# Production environment variables for Railway deployment
# These are baked into the build at compile time

# Backend API URL (Railway backend service)
NEXT_PUBLIC_API_URL=https://trading-agent-la-falange-production.up.railway.app

# WebSocket URL for real-time price streaming
NEXT_PUBLIC_WS_URL=wss://trading-agent-la-falange-production.up.railway.app
